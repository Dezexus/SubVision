paddleocr==2.9.1
scipy>=1.11.0
einops>=0.7.0
timm>=0.9.0
addict>=2.4.0
imageio-ffmpeg>=0.4.9
# Optional emotion export — enable with INSTALL_EMOTION_DEPS=1 at build time:
# gigaam
# pyannote.audio>=3.1
# torch + torchvision: install with CUDA index (see Dockerfile worker stage)
