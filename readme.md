# SubVision ⚡

SubVision — это инструмент на базе ИИ для автоматического извлечения субтитров напрямую из видеофайлов. Он использует OCR (оптическое распознавание символов) для чтения текста с кадров и генерирует стандартные файлы субтитров в формате `.srt`.

## 🚀 Основные возможности

-   **OCR из видео**: Прямое извлечение текста из видеофайлов (`.mp4`, `.mov`, `.mkv` и др.).
-   **Веб-интерфейс**: Удобный и интуитивно понятный интерфейс, созданный с помощью Gradio.
-   **Выбор зоны субтитров (ROI)**: Возможность вручную указать область на экране, где появляются субтитры, для повышения точности.
-   **AI-редактура (Gemma)**: Опциональная коррекция грамматики, пунктуации и орфографии в распознанном тексте с помощью LLM-модели Google Gemma.
-   **Предварительная обработка**: Автоматическая гамма-коррекция для улучшения читаемости текста и Auto-Upscale для повышения качества распознавания в маленьких ROI.
-   **Стабилизация текста**: Встроенный механизм для устранения "мерцания" и ошибок распознавания между кадрами.
-   **Поддержка GPU**: Ускорение процессов OCR и AI-коррекции с помощью NVIDIA GPU.
-   **Простота запуска**: Проект полностью контейнеризирован с помощью Docker, что обеспечивает быстрый старт на любой системе.

## 🛠️ Технологический стек

-   **Распознавание (OCR)**: PaddlePaddle (PaddleOCR)
-   **AI-коррекция (LLM)**: Google Gemma (через `llama-cpp-python`)
-   **Обработка видео**: OpenCV
-   **Веб-интерфейс**: Gradio
-   **Окружение**: Docker, Docker Compose

## ⚙️ Как это работает

Процесс извлечения субтитров состоит из нескольких ключевых этапов:

1.  **Захват кадров**: Видео обрабатывается с заданным шагом (например, каждый 2-й кадр).
2.  **Предварительная обработка**: К выбранной зоне (ROI) применяются фильтры (гамма-коррекция, апскейл) для улучшения качества изображения.
3.  **Распознавание (OCR)**: Подготовленное изображение передается в движок PaddleOCR для извлечения текста.
4.  **Стабилизация**: Результаты с нескольких последовательных кадров накапливаются в буфере. Самый стабильный и повторяющийся текст выбирается в качестве итогового, что устраняет случайные ошибки.
5.  **Формирование субтитра**: Когда текст на экране меняется, для предыдущего блока формируются временные метки (начало и конец).
6.  **AI-редактура (опционально)**: После завершения OCR весь полученный текст отправляется одним пакетом в модель Gemma для исправления ошибок.
7.  **Генерация SRT**: Финальный, исправленный текст сохраняется в `.srt` файл.

## 📦 Установка и запуск

Проект разработан для запуска в Docker-контейнере. Это обеспечивает изоляцию зависимостей и простоту развертывания.

### Требования

-   [Docker](https://www.docker.com/get-started)
-   [Docker Compose](https://docs.docker.com/compose/install/) (обычно идет в комплекте с Docker Desktop)
-   Драйверы NVIDIA для вашей ОС, если вы планируете использовать GPU.

### Запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/your-username/subvision.git
    cd subvision
    ```

2.  **Запустите с помощью Docker Compose:**
    ```bash
    docker-compose up --build
    ```
    Эта команда соберет Docker-образ (если он еще не собран) и запустит контейнер. Сборка может занять значительное время, так как компилируются "тяжелые" библиотеки.

3.  **Откройте интерфейс:**
    Перейдите в браузере по адресу `http://localhost:7860`.

## 📖 Как использовать

1.  **Загрузите видео**: Нажмите на блок "1. Видео файл" и выберите ваш видеофайл.
2.  **Выберите кадр**: С помощью слайдера "2. Выбор кадра" найдите момент в видео, где на экране присутствуют субтитры.
3.  **Укажите зону (ROI)**: В редакторе "3. Зона субтитров" выделите красной кистью прямоугольную область, где появляется текст. Превью в блоке "Глазами нейросети" покажет, как OCR-движок "видит" эту область.
4.  **Настройте параметры**:
    -   **ИИ Редактура (Gemma)**: Включите, если хотите автоматически исправить текст.
    -   **Языки**: по умолчанию распознавания настроено на англ. (возможно расширение)
    -   **Gamma**: Отрегулируйте контрастность, если текст плохо читается.
5.  **Запустите процесс**: Нажмите кнопку **"🚀 СТАРТ"**.
6.  **Отслеживайте прогресс**: В текстовом поле "Лог" будет отображаться информация о ходе выполнения, а в таблице "Распознанные субтитры" — результаты в реальном времени.
7.  **Скачайте результат**: После завершения обработки в блоке "Скачать SRT" появится файл, готовый для скачивания.